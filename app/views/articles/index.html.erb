<div class="w-full max-w-2xl mx-auto">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div id="articles" class="grid grid-cols-1 md:grud-cols-2 gap-4">
    <%= render @articles %>
  </div>
</div>
