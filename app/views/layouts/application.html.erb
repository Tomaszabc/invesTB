<!DOCTYPE html>
<html>
  <head>
    <title>InvesTB</title>
    <meta name="Blog inwestycyjny" content="Analiza sytuacji na giełdzie, akcje, surowce, strach i chciwość, strategie, własne przemyślenia. Kupuj tanio, sprzedawaj drogo.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
  <%= render 'shared/navbar' %>
  <%= render 'shared/top-bar/top-bar'%>

    <main class="container mx-auto mt-8 px-3  max-w-4xl mb-16">
      <%= yield %>
      
    </main>
  </body>
</html>
