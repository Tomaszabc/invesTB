<%= form_with(model: [article, Comment.new], local: false) do |form| %>
  <% if form.object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(form.object.errors.count, "error") %> Nie zapisano ponieważ:</h2>
      <ul>
        <% form.object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: "comment_content", class: "ckeditor" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<script src="//cdn.ckeditor.com/4.22.1/standard-all/ckeditor.js"></script>
<script>
  document.addEventListener("DOMContentLoaded", function() {
    CKEDITOR.replace('comment_content');
  });
</script>